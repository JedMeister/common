#include <boot>
#include <net>
#include <console>

wget
curl
rsync

#ifndef CHROOT_ONLY
di-live
lvm2            /* di-live recommends */
eject           /* di-live recommends */
#endif

whiptail        /* di-live recommends */
console-tools   /* di-live recommends */
kbd             /* di-live recommends */
file            /* di-live recommends */

confconsole
console-tools   /* confconsole recommends */
kbd             /* confconsole recommends */
resolvconf      /* confconsole recommends */

#ifndef CHROOT_ONLY
di-live         /* confconsole recommends */
#endif

tklbam
hubdns
inithooks
python-dialog
turnkey-pylib
turnkey-sysinfo
turnkey-version

cron
cron-apt

etckeeper
git-core

lsb-release
localepurge
man-db
screen
nano
ntp
ncurses-term    /* support additional $TERM values */

shellinabox
stunnel4        /* provide ssl to shellinabox */

webmin
webmin-theme-stressfree
webmin-net
webmin-software
webmin-useradmin
webmin-passwd
webmin-shell
webmin-custom
webmin-file
webmin-text-editor
#ifndef CHROOT_ONLY
webmin-lvm
#endif
webmin-tklbam
webmin-updown
unzip           /* webmin-updown recommends */

logrotate
rsyslog
webmin-syslog

iptables
webmin-firewall

ssh
webmin-sshd

bsd-mailx
postfix
webmin-postfix

#ifndef CHROOT_ONLY
acpi-support-base
#endif

